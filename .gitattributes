# Normalize line endings across Windows and Mac
* text=auto eol=lf

# Force LF on shell scripts (critical for Mac execution)
*.sh text eol=lf diff
.githooks/* text eol=lf diff

# Binary files
*.png binary
*.ico binary
*.zip binary
