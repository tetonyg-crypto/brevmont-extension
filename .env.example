# Copy to `.env` for local builds. WXT loads env vars prefixed with WXT_.

# Get your DSN from https://sentry.io — create a Browser JavaScript project
# Free tier: 5,000 events/month (more than enough for founding pilots)
# Until this is set, error tracking is disabled but everything else works
WXT_SENTRY_DSN=
